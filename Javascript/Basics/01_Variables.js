const accountId = 23456;
let accountName = "Safaraccount";
var accountPassWord = "sdfsdf";
accountState = "";
let accountAddress;

// accountId = 2345; // NOT ALLOWED

// Apart from const let var or the variable without any keyword are changable


// if inintial value is not given than the variable is marked as a undefined.

// prefer not to use var
// because of the scope issue(Blocked scope and functional scope)


console.table([accountId,accountName,accountPassWord,accountState,accountAddress]);

console.log(Hoisting)
const Hoisting = 2;

  